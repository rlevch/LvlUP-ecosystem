-- =============================================================================
-- Supabase Realtime — создание схемы _realtime
-- =============================================================================
-- Supabase Realtime (сервис) требует эту схему для хранения своего состояния
-- =============================================================================

\set pguser `echo "$POSTGRES_USER"`

CREATE SCHEMA IF NOT EXISTS _realtime;
ALTER  SCHEMA _realtime OWNER TO :pguser;
